namespace SofaStream.Domain.Common;

/// <summary>
/// Defines a contract for domain events within the SofaStream system.
/// Domain events represent significant business occurrences that have already happened 
/// and may trigger side effects across different parts of the system.
/// </summary>
public interface IDomainEvent
{
    
}